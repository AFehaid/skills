# Core engine dependencies for transcribe_diarize.py
# NOTE: install a torch build that matches your CUDA for GPU speed, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# CPU-only torch also works (slower): pip install torch
faster-whisper>=1.1
ctranslate2>=4.4
pyannote.audio>=4.0
torch>=2.2
torchaudio>=2.2
soundfile>=0.12
huggingface_hub>=0.26
# ffmpeg must be installed separately and available on PATH (system package).
